#include <iostream> // Thư viện cho phép nhập (cin) và xuất (cout)
using namespace std; // Giúp viết cout, cin mà không cần std::cout

int main() {
    // ================================
    // 👋 BẮT ĐẦU LẬP TRÌNH C++ CƠ BẢN
    // ================================

    // 1️⃣ Khai báo biến
    int a = 5;       // kiểu int (số nguyên)
    double b = 2.5;  // kiểu double (số thực)
    char c = 'A';    // kiểu ký tự
    string name;     // kiểu chuỗi (cần #include <string> nếu dùng)
    
    // ✅ Kết thúc chương trình
    return 0;
}
